# Conquiz - Архитектура проекта

## 📐 Общая схема

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              UNITY SCENE                                 │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                         QuizSceneSetup                           │    │
│  │                    (Entry Point - Start())                       │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                   │                                      │
│           ┌───────────────────────┼───────────────────────┐             │
│           │                       │                       │             │
│           ▼                       ▼                       ▼             │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐     │
│  │   QuizCanvas    │    │  DebugCanvas    │    │    Managers     │     │
│  │  (sorting: 10)  │    │ (sorting: 100)  │    │                 │     │
│  └────────┬────────┘    └────────┬────────┘    └────────┬────────┘     │
│           │                      │                      │               │
│           ▼                      ▼                      ▼               │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐     │
│  │ QuizUIController│    │SessionDebugPanel│    │QuizSessionCtrl  │     │
│  │                 │    │                 │    │HumanAnswerProv  │     │
│  │ - QuizPanel     │    │ - HvB Button    │    │BotAnswerProvider│     │
│  │ - MCQPanel      │    │ - BvB Button    │    │                 │     │
│  │ - NumericPanel  │    │ - Auto/Manual   │    │                 │     │
│  │ - ResultsPanel  │    │ - Status Text   │    │                 │     │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Жизненный цикл Unity (Lifecycle)

```
Unity запускает игру
         │
         ▼
┌─────────────────┐
│     Awake()     │  ← Первый вызов, инициализация
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│     Start()     │  ← После Awake всех объектов
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    Update()     │  ← Каждый кадр (~60 раз/сек)
│  FixedUpdate()  │  ← Каждый физический шаг
│  LateUpdate()   │  ← После всех Update
└────────┬────────┘
         │
    (игра идёт)
         │
         ▼
┌─────────────────┐
│   OnDestroy()   │  ← Объект уничтожается
└─────────────────┘
```

### Сравнение с Android:

```
Android Activity          Unity MonoBehaviour
─────────────────        ────────────────────
onCreate()          →    Awake() + Start()
onResume()          →    OnEnable()
onPause()           →    OnDisable()
onDestroy()         →    OnDestroy()
─────────────────        ────────────────────
(нет аналога)       →    Update() - каждый кадр!
```

---

## 🎯 Поток выполнения Quiz сессии

```
┌────────────────────────────────────────────────────────────────────────┐
│                         QUIZ SESSION FLOW                               │
├────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   [User clicks "Human vs Bot"]                                          │
│              │                                                          │
│              ▼                                                          │
│   SessionDebugPanel.StartHumanVsBot()                                   │
│              │                                                          │
│              ▼                                                          │
│   QuizSessionController.StartSession(human, bot)                        │
│              │                                                          │
│              ▼                                                          │
│   ┌─────────────────────────────────────────────────────────────────┐  │
│   │                    RunSessionCoroutine()                         │  │
│   │  ┌───────────────────────────────────────────────────────────┐  │  │
│   │  │  ROUND 1: MCQ                                              │  │  │
│   │  │                                                            │  │  │
│   │  │  1. quizUI.ShowMcqQuestion(question)                       │  │  │
│   │  │  2. human.ProvideAnswerAsync() ─┬─► WaitForBothAnswers()   │  │  │
│   │  │  3. bot.ProvideAnswerAsync()   ─┘                          │  │  │
│   │  │  4. EvaluateMcqRound()                                     │  │  │
│   │  │     ├─► One correct, one wrong → WINNER DECIDED            │  │  │
│   │  │     └─► Tie (both correct/wrong) → GO TO ROUND 2           │  │  │
│   │  └────────────────────────────────────────────────────────────┘  │  │
│   │                              │                                    │  │
│   │                         [If tie]                                  │  │
│   │                              ▼                                    │  │
│   │  ┌───────────────────────────────────────────────────────────┐  │  │
│   │  │  ROUND 2: NUMERIC                                          │  │  │
│   │  │                                                            │  │  │
│   │  │  1. quizUI.ShowNumericQuestion(question)                   │  │  │
│   │  │  2. human.ProvideAnswerAsync() ─┬─► WaitForBothAnswers()   │  │  │
│   │  │  3. bot.ProvideAnswerAsync()   ─┘                          │  │  │
│   │  │  4. EvaluateNumericRound()                                 │  │  │
│   │  │     └─► Winner = closest to correct answer                 │  │  │
│   │  │         (tie-break by response time)                       │  │  │
│   │  └────────────────────────────────────────────────────────────┘  │  │
│   │                              │                                    │  │
│   │                              ▼                                    │  │
│   │  ┌───────────────────────────────────────────────────────────┐  │  │
│   │  │  SHOW RESULTS                                              │  │  │
│   │  │                                                            │  │  │
│   │  │  1. quizUI.ShowFinalResultAndWait()                        │  │  │
│   │  │  2. Wait for "Continue" button click                       │  │  │
│   │  │  3. Session ends                                           │  │  │
│   │  └────────────────────────────────────────────────────────────┘  │  │
│   └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Компоненты и их ответственности

### QuizSceneSetup.cs
```
┌─────────────────────────────────────────────────────────────────┐
│                        QuizSceneSetup                            │
├─────────────────────────────────────────────────────────────────┤
│  Responsibilities:                                               │
│  ✓ Создание всех UI элементов программно                        │
│  ✓ Загрузка вопросов из Resources                               │
│  ✓ Создание и конфигурация всех компонентов                     │
│  ✓ Связывание компонентов через reflection                      │
│  ✓ Глобальная обработка F1 для toggle debug panel               │
├─────────────────────────────────────────────────────────────────┤
│  Key Methods:                                                    │
│  • Start() - entry point                                         │
│  • CreateQuizCanvas() - создаёт Quiz UI                          │
│  • CreateDebugCanvas() - создаёт Debug Panel                     │
│  • WireReferences() - связывает компоненты                       │
├─────────────────────────────────────────────────────────────────┤
│  Creates:                                                        │
│  • QuizCanvas (GameObject + Canvas + QuizUIController)           │
│  • DebugCanvas (GameObject + Canvas + SessionDebugPanel)         │
│  • HumanAnswerProvider                                           │
│  • BotAnswerProvider                                             │
│  • QuizSessionController                                         │
└─────────────────────────────────────────────────────────────────┘
```

### QuizSessionController.cs
```
┌─────────────────────────────────────────────────────────────────┐
│                     QuizSessionController                        │
├─────────────────────────────────────────────────────────────────┤
│  Responsibilities:                                               │
│  ✓ Управление логикой игровой сессии                            │
│  ✓ Координация раундов (MCQ → Numeric)                          │
│  ✓ Сбор ответов от игроков                                      │
│  ✓ Определение победителя                                       │
│  ✓ Обработка таймаутов                                          │
├─────────────────────────────────────────────────────────────────┤
│  Dependencies:                                                   │
│  ← QuizUIController (показ UI)                                   │
│  ← IAnswerProvider[] (получение ответов)                         │
│  ← McqQuestionData[] (вопросы MCQ)                               │
│  ← NumericQuestionData[] (вопросы Numeric)                       │
├─────────────────────────────────────────────────────────────────┤
│  Events:                                                         │
│  → OnSessionComplete(SessionResult)                              │
└─────────────────────────────────────────────────────────────────┘
```

### QuizUIController.cs
```
┌─────────────────────────────────────────────────────────────────┐
│                       QuizUIController                           │
├─────────────────────────────────────────────────────────────────┤
│  Responsibilities:                                               │
│  ✓ Отображение вопросов (MCQ/Numeric)                           │
│  ✓ Управление таймером                                          │
│  ✓ Обработка кликов на кнопки                                   │
│  ✓ Анимации переходов                                           │
│  ✓ Отображение результатов                                      │
│  ✓ Обновление статуса игроков                                   │
├─────────────────────────────────────────────────────────────────┤
│  UI Elements (assigned via reflection):                          │
│  • quizPanel - главная панель                                    │
│  • mcqPanel - панель с кнопками MCQ                              │
│  • numericPanel - панель ввода числа                             │
│  • resultsPanel - панель результатов                             │
│  • playerBadgeLeft/Right - иконки игроков                        │
│  • timerText, timerFillImage - таймер                            │
├─────────────────────────────────────────────────────────────────┤
│  Events (callbacks):                                             │
│  → OnMcqAnswerSubmitted(int index, float time)                   │
│  → OnNumericAnswerSubmitted(float value, float time)             │
│  → OnTimerExpired()                                              │
│  → OnContinuePressed()                                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔗 Связи между компонентами

```
┌──────────────────┐         ┌──────────────────┐
│ SessionDebugPanel│────────▶│QuizSessionCtrl   │
│                  │ starts  │                  │
│ • HvB Button     │ session │ • StartSession() │
│ • BvB Button     │         │                  │
└──────────────────┘         └────────┬─────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    │                 │                 │
                    ▼                 ▼                 ▼
           ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
           │QuizUIController│  │HumanAnswer   │  │BotAnswer     │
           │               │  │Provider      │  │Provider      │
           │ shows UI      │  │              │  │              │
           │ handles input │◀─│ gets input   │  │ generates    │
           │               │  │ from UI      │  │ answers      │
           └───────────────┘  └──────────────┘  └──────────────┘
                                                       │
                                                       ▼
                                              ┌──────────────┐
                                              │DebugBotSetts │
                                              │ (Singleton)  │
                                              │              │
                                              │ • Auto/Manual│
                                              │ • Override   │
                                              │   values     │
                                              └──────────────┘
```

---

## 📱 UI Иерархия

```
Scene
├── Main Camera
├── EventSystem
├── QuizManager
│   └── BotAnswerProvider (child component at runtime)
│
├── QuizCanvas (created at runtime)
│   └── QuizPanel
│       ├── Header
│       │   ├── PlayerBadgeLeft
│       │   │   ├── Label ("YOU")
│       │   │   └── Status ("Thinking...")
│       │   ├── Spacer
│       │   ├── Timer
│       │   │   ├── BgRing
│       │   │   ├── FillRing
│       │   │   └── TimerText
│       │   ├── Spacer
│       │   └── PlayerBadgeRight
│       │       ├── Label ("OPPONENT")
│       │       └── Status ("Thinking...")
│       │
│       ├── QuestionSection
│       │   ├── RoundIndicator
│       │   ├── CategoryText
│       │   ├── Separator
│       │   └── QuestionText
│       │
│       ├── MCQPanel
│       │   ├── Row1
│       │   │   ├── McqButton0 ("A) ...")
│       │   │   └── McqButton1 ("B) ...")
│       │   └── Row2
│       │       ├── McqButton2 ("C) ...")
│       │       └── McqButton3 ("D) ...")
│       │
│       ├── NumericPanel (hidden initially)
│       │   ├── InputField
│       │   ├── UnitText
│       │   └── SubmitButton
│       │
│       └── ResultsPanel (hidden initially)
│           ├── PlayerResultText
│           ├── OpponentResultText
│           ├── OutcomeText
│           └── ContinueButton
│
└── DebugCanvas (created at runtime, sortingOrder: 100)
    └── SessionDebugPanel
        ├── Title
        ├── HumanVsBotButton
        ├── BotVsBotButton
        ├── Separator
        ├── ModeLabel
        ├── ModeButtons (Auto/Manual)
        ├── BotStatusText
        └── ManualControls (when Manual mode)
```

---

## 🎨 Canvas Sorting Order

```
Layer Order (higher = on top):

100 ─┬─ DebugCanvas ────────── Always on top (clickable during quiz)
     │
 10 ─┼─ QuizCanvas ─────────── Quiz UI
     │
  0 ─┴─ (Default) ──────────── Background elements
```

---

## 🔄 Event Flow при ответе

```
[User clicks MCQ button]
         │
         ▼
QuizUIController.OnMcqButtonClicked(index)
         │
         ├─► hasAnswered = true
         ├─► isTimerRunning = false
         ├─► MarkPlayerAnswered()  ──► PlayerBadgeLeft.SetState(Answered)
         │
         ▼
OnMcqAnswerSubmitted?.Invoke(index, responseTime)
         │
         ▼
HumanAnswerProvider (listening)
         │
         ├─► Stores answer
         └─► Completes TaskCompletionSource
         │
         ▼
QuizSessionController.WaitForBothAnswers() returns
         │
         ▼
EvaluateMcqRound() called
         │
         ├─► Compare answers
         ├─► Determine winner or tie
         └─► Update UI with results
```

---

## 📝 Добавление нового функционала

### Чеклист для новой фичи:

1. **Определи, что нужно:**
   - [ ] Новый тип вопроса? → ScriptableObject + UI Panel
   - [ ] Новый режим игры? → SessionDebugPanel button + Controller method
   - [ ] Новый UI элемент? → QuizSceneSetup method
   - [ ] Новая логика бота? → BotAnswerProvider modification

2. **Где писать код:**
   - UI создание → `QuizSceneSetup.cs`
   - UI логика → `QuizUIController.cs`
   - Игровая логика → `QuizSessionController.cs`
   - Ввод игрока → `HumanAnswerProvider.cs`
   - Логика бота → `BotAnswerProvider.cs`
   - Debug функции → `SessionDebugPanel.cs`

3. **Тестирование:**
   - [ ] Добавь Debug.Log() в ключевые места
   - [ ] Проверь в Console на ошибки
   - [ ] Используй Inspector для проверки значений runtime

